# Filesystem example workspace

This is synthetic local content for the MCPZT filesystem example. Reading this
file is allowed. Writing inside this directory requires approval; paths outside
this directory are denied before dispatch. Never put real secrets in a demo.
